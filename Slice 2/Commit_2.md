# ST-554-Project3-Commit 2

Using the pipeline and transformed data created in previous steps. I had issues with the linear regression model being read so I added in the for loop to check. The notifier didn't go off so the LR Model was read in but not needed I am guessing. My code is probably incorrect but no solution is working. I fot the cross validator which takes a long time to load and will be used in the next parts. 

There are a lot of errors, however, I was messing around and it seems to be a result of the numbers being so close to zero or specifically less than one.
